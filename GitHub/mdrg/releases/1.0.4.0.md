# Changelog | v1.0.4.0

- `GameData` is now a partial class
- `GameData` fields are now separated between multiple scripts, meaning multiple classes are compiled into one: allowing for better expandability