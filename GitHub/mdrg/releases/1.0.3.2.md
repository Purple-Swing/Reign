# Changelog | v1.0.3.2

- `SceneLoadSystem` now uses a `FadeDirection` enum to decide if the loading screen will fade in or out 
- `SceneLoadSystem` now uses `unscaledDeltaTime` so when transitioning, meaning you can now load scenes when the time scale is set to zero.
- Updated the Markdown Release Generator to use the path relative to the python file.
